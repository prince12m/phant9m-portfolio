export {
  projects,
  getAllProjectSlugs,
  getFeaturedProjects,
  getProject,
  getProjectStatusMeta,
} from "@/data/projects";

export type { Project, ProjectLink, ProjectStatus } from "@/data/projects";
